application:
  mail: smtp

javamail:
  authentication:
    username: ${SMTP_USER}
    password: ${SMTP_PASSWORD}
  properties:
    mail.smtp.host: ${SMTP_HOST}
    mail.smtp.port: 587
    mail.smtp.auth: true
    mail.smtp.starttls.enable: true
