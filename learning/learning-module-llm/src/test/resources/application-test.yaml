micronaut.application.storage.location: ./build/

application:
  ai:
    ollama:
      model: qwen2.5-coder:1.5b
      force-augmentation: false
    vectors:
      storage: ${micronaut.application.storage.location}/vectors
