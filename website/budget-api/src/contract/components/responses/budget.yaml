type: object
required:
  - period
  - expenses
properties:
  period:
    $ref: '#/components/schemas/date-range'
  expenses:
    type: array
    items:
      $ref: '#/components/schemas/expense-response'
